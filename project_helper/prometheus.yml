global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "rust_assistant"
    static_configs:
      - targets: ["host.docker.internal:9090"]  # app 的 metrics server
    metrics_path: /metrics
